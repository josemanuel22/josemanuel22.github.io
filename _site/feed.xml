<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" hreflang="en" /><updated>2024-02-27T23:55:25+01:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">José Manuel de Frutos website</title><subtitle>Mathematician and Computer Scientist, Software Engineer at Devo.
</subtitle><entry><title type="html">Why Julia is so fast?</title><link href="http://localhost:4000/blog/2023/why_Julia_is_so_fast/" rel="alternate" type="text/html" title="Why Julia is so fast?" /><published>2023-07-01T00:00:00+02:00</published><updated>2023-07-01T00:00:00+02:00</updated><id>http://localhost:4000/blog/2023/why_Julia_is_so_fast</id><content type="html" xml:base="http://localhost:4000/blog/2023/why_Julia_is_so_fast/"><![CDATA[<p>I have always wanted to understand what makes one programming language faster than another. The problem is that in order to know this, one must understand in detail how the specific programming language one wishes to study is implemented. This is often a complicated task, as there are millions of books that explain the basics of the syntax of a programming language, but there are hardly any books that explain the technical and theoretical details behind the design of a language. Generally, to acquire this knowledge, one must have spent a lot of time programming in that language, get to know the project from within, read thousands of blogs and questions and answers on Stack Overflow, etc.</p>

<p>I’ve always found this to be a pity because I believe that the fundamental technical aspects behind a programming language can be summarized in about a hundred pages. This would drastically accelerate the path to becoming an ‘expert’ in that language. I don’t aim for such a thing with this blog, but in the case of the Julia project, although it lacks such a book, it is possible to find references that concentrate and describe the fundamental aspects behind the language design. On the other hand, the fact that it is a language oriented towards performance makes it ideal for studying.</p>

<h1 id="julia-implementation">Julia Implementation</h1>

<p>Julia is designed to generate efficient native code during runtime. The Julia compiler is an optimizing just-in-time compiler structured in three phases:</p>

<ol>
  <li>Source code is first parsed into abstract syntax trees [2].</li>
  <li>Those trees are then lowered into an intermediate representation that is used for Julia level optimizations.</li>
  <li>Once those optimizations are complete, the code is translated into LLVM IR and machine code is generated by LLVM.</li>
</ol>

<p>With the exception of the standard library which is pre-compiled, all Julia code executed by a running program is compiled on demand. The compiler is relatively simple: it is a method-based JIT without compilation tiers; once methods are compiled they are not changed as Julia does not support deoptimization with on-stack replacement.</p>

<p>Memory is managed by a stop-the-world, non-moving, markand-sweep garbage collector [3,4]. The mark phase can be executed in parallel. The collector has a single old generation for objects that survive a number of cycles. It uses a <a href="https://en.wikipedia.org/wiki/Shadow_stack">shadow stack</a> to record pointers in order to be precise.</p>

<h2 id="method-specialization">Method Specialization</h2>

<p><strong>Julia’s compilation strategy relies on runtime type information</strong>: Every time a method is called with a new tuple of argument types, the method is specialized to these specific types. These are the advantage of such a strategy:</p>

<ul>
  <li>
    <p>Optimizing methods at invocation time in Julia provides the Just-In-Time (JIT) compiler with important information, such as the known <strong>memory layout</strong> of all arguments. This enables optimizations like unboxing and direct field access.</p>
  </li>
  <li>
    <p>Specialization in Julia allows for devirtualization, which replaces method dispatch with direct calls to a specialized method. This replacement reduces dispatch overhead and enables inlining, leading to improved performance.</p>
  </li>
</ul>

<p>The biggest disadvantage of method specialization is that the compilation process can be slow. To address this issue, the results are cached, and methods are only compiled the first time they are called with a new type.</p>

<h2 id="type-inference">Type inference</h2>

<p>The design of the type system is one of the most subtle aspects and a key reason for its efficiency. On one hand, it possesses a rich typing structure, but at the same time, it is not overly complex as this could hinder and complicate the compiler’s task of code optimization.</p>

<p>Type information plays a crucial role in optimizing code in Julia. The compiler analyzes the flow of data and applies constraints to determine the types involved. Type requirements must be satisfied at function calls and assignments. Through intra- and interprocedural analysis, types are inferred based on the specific arguments provided, even in cases involving recursion. However, to ensure the analysis process doesn’t become infinite for methods that could potentially have an infinite number of argument or return types, Julia imposes a limit on the size of inferred types. This limitation ensures that the set of possible types remains finite and guarantees termination of the analysis.</p>

<h2 id="method-inlining">Method Inlining</h2>

<p>Inlining in Julia refers to the replacement of a function call with the body of the called function. In Julia, inlining is efficient due to its synergy with specialization and type inference. Type-stable method bodies can be effectively inlined, and inlining aids type inference by providing additional context. Inlined code can optimize by eliminating unnecessary branches and propagating precise type information. However, inlining may incur high memory costs and additional compilation time, leading to pragmatic heuristics that limit its application.</p>

<h2 id="object-unboxing">Object Unboxing</h2>

<p>In Julia, due to its dynamic nature, a variable can hold values of multiple types. Consequently, in the general case, values are allocated on the heap with a tag that specifies their type. However, the optimization of unboxing allows for direct manipulation of values. Several design choices contribute to this optimization. First, concrete types in Julia are final, meaning they specify both the size and layout of a value. This differs from languages like Java or TypeScript that have subtyping. Additionally, Julia does not have a null value, which eliminates the need for an extra tag for primitive values. As a result, values such as integers and floats can always be stored unboxed. While repeated boxing and unboxing can be expensive, and unboxing may not be possible for recursive data structures despite the presence of type information, heuristics are used to determine when to perform this optimization, similar to inlining.</p>

<h2 id="multiple-dispatch">Multiple dispatch</h2>

<p>Julia uses multiple dispatch extensively, allowing extension of functionality by means of overloading. Each function can consist of an arbitrarily large number of methods. Each of these methods declares what types it can handle, and Julia will dispatch to whichever method is most specific for a given call. Multiple dispatch is
omnipresent, virtually every operation in Julia involves dispatch.</p>

<p>Multiple dispatch is the standout feature of Julia’s design. Its synergy with specialization is essential for understanding the language’s performance and its efficient ability to devirtualize and inline code. Furthermore, one of the promises of multiple dispatch is that it allows extending existing behavior with new implementations.</p>

<p>By default, Julia employs LLVM at optimization level O2, and disabling LLVM optimizations significantly slows down the code (1.1x and 7.1x slower) However turning off type inference or devirtualization capabilities will have and impact on performance much bigger (5x and 2000x slower). Clearly the type inference, devirtualization and code inlining are the most important optimizations in Julia together with the capabilities of multiple dispatching.</p>

<h2 id="references">References</h2>

<p>This is a summary of the ideas developed in the work <a href="https://dl.acm.org/doi/pdf/10.1145/3276490">Bezanson, Jeff, et al. “Julia: Dynamism and performance reconciled by design.” Proceedings of the ACM on Programming Languages 2.OOPSLA (2018): 1-23.</a></p>

<p>Also there a some ideas inspired by the following blog posts and papers:</p>

<p>[1] Bezanson, Jeff, et al. “Julia: Dynamism and performance reconciled by design.” Proceedings of the ACM on Programming Languages 2.OOPSLA (2018): 1-23.</p>

<p>[2] https://www.abstractsyntaxseed.com/blog/antlr4-autocomplete-library/introduction</p>

<p>[3] https://www.geeksforgeeks.org/mark-and-sweep-garbage-collection-algorithm/</p>

<p>[4] Richard Jones, Antony Hosking, and Eliot Moss. 2011. The Garbage Collection Handbook: The Art of Automatic Memory Management (1st. ed.). Chapman &amp; Hall/CRC.</p>

<p>[5] https://en.wikipedia.org/wiki/Shadow_stack</p>

<p>[6] https://scientificcoder.com/the-art-of-multiple-dispatch</p>

<p>[7] https://viralinstruction.com/posts/badjulia/</p>]]></content><author><name></name></author><category term="sample-posts" /><category term="dynamic-languages," /><category term="just-in-time-compilation," /><category term="performance," /><category term="programming-languages," /><category term="type-inference" /><summary type="html"><![CDATA[Summary and main concepts of the paper, Julia Dynamism and performance reconciled by design by Bezanson, Jeff, et al.]]></summary></entry><entry><title type="html">Formulas of Brion, Lawrence and Varchenko on rational generating functions for cones.</title><link href="http://localhost:4000/blog/2023/formulas_Brion_Lawrence_and_Varchenko/" rel="alternate" type="text/html" title="Formulas of Brion, Lawrence and Varchenko on rational generating functions for cones." /><published>2023-06-01T00:00:00+02:00</published><updated>2023-06-01T00:00:00+02:00</updated><id>http://localhost:4000/blog/2023/formulas_Brion_Lawrence_and_Varchenko</id><content type="html" xml:base="http://localhost:4000/blog/2023/formulas_Brion_Lawrence_and_Varchenko/"><![CDATA[<p>We strive to present two remarkable discoveries in discrete geometry: the formulas established by Michel Brion [1], James Lawrence [2], and Alexander N. Varchenko [3]. Initially, these formulas may appear incredulous, and even after dedicating considerable time to studying them, they continue to evoke a sense of intrigue and fascination.</p>

<h2 id="brion-formula">Brion Formula</h2>

<p>Let us start with some examples. Let us consider the task of enumerating all positive integers. Despite their infinite nature, we can represent them succinctly using a generating function:</p>

<p>\begin{equation}
x^{1} + x^{2} + x^{3} + … = \sum_{k&gt;0}x^{k}= \dfrac{x}{1-x}
\end{equation}</p>

<p>Similarly, all the integers less than or equal to \(5\),
\begin{equation}
…+x^{-1} + x^{0} + x^{1} + x^{2} + … = \sum_{k≤ k}x^{k}= \dfrac{x^{5}}{1-x^{-1}}.
\end{equation}</p>

<p>Adding the rational functions leads to a miraculous cancellation,</p>

<p>\begin{equation}
\dfrac{x}{1-x} + \dfrac{x^{5}}{1-x^{-1}} = \dfrac{x}{1-x} + \dfrac{x^{6}}{x-1} = \dfrac{x - x^{6}}{1-x} = x + x^{2} + x^{3} + x^{4} +x^{5}.
\end{equation}</p>

<p>The sum of rational functions, which captures the essence of two infinite series, remarkably simplifies into a polynomial expression, representing a finite series. This fascinating phenomenon can be seen as a special case of a theorem formulated by Michel Brion for the one dimensional case. In a certain sense, the <strong>sum of these generating functions acts as the intersection of sets</strong>.</p>

<p>If we focus on the two-dimensional scenario, the equivalence of the generating functions from before can be found in the generating functions associated with the cones formed at each vertex generated by the edges at that vertex. Once more, the sum of rational functions simplifies to a polynomial, capturing exactly the integer points that lie within the quadrilateral.</p>

<p>Brion’s theorem says that this magic happens for any polytope \(\mathcal{P}\) in any dimension \(d\), provided that \(\mathcal{P}\) has rational vertices. Let be \(\mathcal{K_v}\) denote the vertex cone at vertex \(v\), that is the cone with apex \(v\) and generators the edge directions emanating from \(v\). The generating function will be,</p>

<p>\begin{equation}
\sigma_{\mathcal{K_v}}(x) = \sum_{m\in \mathcal{K_v}\cap \mathbb{Z}^{d}}x^{m}.
\end{equation}</p>

<p>The generating function for such a cone is a rational function. According to Brion’s Formula, the rational functions that correspond to the integer points within each vertex cone combine together to yield the polynomial \(\sigma_{\mathcal{P}}(x)\), which encapsulates the information about the integer points in the polytope \(\mathcal{P}\),
\begin{equation}
\sigma_{\mathcal{P}}(x) = \sum_{v\in \mathcal{P}} \sigma_{\mathcal{K_v}}(x).
\end{equation}</p>

<h2 id="james-lawrence-and-to-alexander-varchenko-formula">James Lawrence and to Alexander Varchenko Formula</h2>

<p>Another theorem, independently discovered by James Lawrence and Alexander Varchenko, demonstrates a similar collapsing behavior of generating functions associated with cones. Lawrence–Varchenko procedure is the following. First, we choose a direction vector \(\epsilon\) that is not perpendicular to any edge of the quadrilateral \(\mathcal{Q}\). At each vertex \(v\) of \(\mathcal{Q}\), we create a cone (which may not be closed) by using the edge directions \(m\). If the dot product of \(m\) and \(\epsilon\) is greater than \(0\) (\(m \cdot \epsilon &gt; 0\)), we consider the nonnegative span of the edge direction. Conversely, if the dot product is less than \(0\) (\(m \cdot \epsilon &lt; 0\)), we consider the negative span of the edge direction.</p>

<p>The Lawrence–Varchenko Formula says that adding the generating functions of these cones with appropriate signs gives the polynomial \(\sigma_{\mathcal{P}}(x)\) encoding the integer points in \(\mathcal{P}\),
\begin{equation}
\sigma_{\mathcal{P}}(x) = \sum_{v\in \mathcal{P}} (-1)^{|E_{v}^{-}(\epsilon)|} \sigma_{\mathcal{K_{\epsilon, v}}}(x).
\end{equation}</p>

<h2 id="proofs">Proofs</h2>

<p>Brion’s original proof of his formula [1] involved the application of the Lefschetz-Riemann-Roch theorem in equivariant K-theory to a singular toric variety. However, there is good news! The remarkable formulas established by Brion and Lawrence-Varchenko now have accessible proofs that rely on counting techniques. This development makes understanding and verifying these formulas much simpler. The proof can be divided in four following steps.</p>

<h4 id="1-proving-that-the-apex-of-the-cone-do-not-need-to-be-rational">1. Proving that the apex of the cone do not need to be rational.</h4>

<p>In the first part we need to prove that we do not require that the apex \(v\) of the cone to be rational, but only that the generators \(w_i\) of the cone be linearly independent vectors in \(\mathbb{Z}^{d}\).</p>

<p>A simple rational \(\mathcal{K}\) cone in \(\mathbb{R}^{d}\) has the form,</p>

<p>\begin{equation}
\mathcal{K} = \left\{v + \sum_{i=1}^{d}\lambda_{i}w_{i} \quad \Big |\quad \lambda_{i}\in  \mathbb{R_{\geq 0}} = v + \sum_{i=1}^{d} \mathbb{R_{\geq 0}}w_{i}\right\},
\end{equation}
where \(w_{1}, \ldots, w_{d}\in \mathbb{Z}^{d}\) are l.i.</p>

<p>The idea behind proving this is to tile a simple cone by translating its fundamental parallelepiped, denoted as \(\mathcal{P}\). By summing up the generating functions of all the translations of \(\mathcal{P}\), we obtain the generating function of the cone, denoted as \(\mathcal{K}\).</p>

<h4 id="2-decomposing-the-cone-into-simplicial-cones">2. Decomposing the cone into simplicial cones.</h4>

<p>If there exist a vector \(\epsilon\) such that \(\epsilon \cdot w_{i} &gt;0\) for all \(i\), then \(\mathcal{K}\) is strictly convex. If \(\mathcal{K}\) is strictly convex, then it may be decomposed into simple cones having pairwise disjoint interiors, each with apex \(v\) and with the generators \(w_{i}\) of \(\mathcal{K}\). We would like to add the generating functions for each cone \(\mathcal{K_i}\) to obtain the generating function for \(\mathcal{K}\). However, some of the cones may have lattice points in common, so we needed to treat the subsequent overcounting.</p>

<p>An elegant approach to address this is by circumventing the issue of overcounting through the process of translating all the cones. There exists a short vector \(s \in \mathbb{R}^{d}\) such that and no facet of any cone contains any integer points. This gives the disjoint irrational decomposition,
\begin{equation}
\mathcal{K}\cap \mathbb{Z}^{d} = \bigcup_{i=1}^{l} \left (s+\mathcal{K_{i}}\right ),
\end{equation}</p>

<p>and so,</p>

<p>\begin{equation}
\sigma_{\mathcal{K}}(x) = \sum_{m\in \mathcal{K}\cap \mathbb{Z}^{d}}^{l} x^{m}=\sum_{i=1}^{l} \sigma_{s+\mathcal{K_{i}}}(x),
\end{equation}
is a rational function.</p>

<h4 id="3-connecting-formal-laurent-series-with-rational-functions">3. Connecting Formal Laurent Series with Rational Functions</h4>

<p>While the Lawrence-Varchenko formula relies on simple cones and Brion’s formula involves strictly convex cones, we can employ even more general cones in the proof of these formulas. For this, we define a rational (closed) halfspace as the convex subset of \(\mathbb{R}^{d}\) that is defined by,
\begin{equation}
\left \{x\in \mathbb{R}^{d} \quad | \quad w\cdot x \geq b \right \},
\end{equation}
A (closed) cone, denoted as \(\mathcal{K}\), is formed by the intersection of a finite number of closed halfspaces, where the boundary hyperplanes of these halfspaces share at least one common point. The apex of \(\mathcal{K}\) is the intersection of these boundary hyperplanes, which is an affine linear subspace. The generating function for the integer points in \(\mathcal{K}\) is the formal Laurent series,
\begin{equation}
\mathcal{S_{\mathcal{K}}} = \sum_{m\in \mathcal{K}}x^{m}.
\end{equation}
This formal series makes sense as a rational function only if \(\mathcal{K}\) is strictly convex, that is, if its apex is a single point.</p>

<p>The following theorem connects the formal Laurent series with rational functions. Furthermore, it states that there exists a unique homomorphism between the two spaces. This crucially enables us to establish Brion’s Formula by manipulating the formal Laurent series associated with each vertex and then employing the aforementioned theorem to obtain the rational function representing the entire simplex.</p>

<p><strong>Theorem 1.</strong> There is a unique homomorphism of \(\mathbb{C}[x_{1}^{\pm 1}, \ldots, x_{d}^{\pm 1}]\)-modules,
\begin{equation}
\rho: PL \rightarrow \mathbb{C}(x_{1}, \ldots, x_{d}),
\end{equation}
s.t. \(\rho(\mathcal{S_{\mathcal{K}}}) = \sigma_{\mathcal{K}}\) for every simple cone \(\mathcal{K}\) in \(\mathbb{R}^{d}\)</p>

<ul>
  <li>
    <p>PL denotes the polyhedral Laurent series, which is the \(\mathbb{C}[x_{1}^{\pm 1}, \ldots, x_{d}^{\pm 1}]\)-submodule of \(\mathbb{C}[[x_{1}^{\pm 1}, \ldots, x_{d}^{\pm 1}]]\) generated by the set of formal series \(\mathcal{S_{\mathcal{K}}}\) where \(\mathcal{K}\) is a simple rational cone.</p>
  </li>
  <li>
    <p>\(\mathbb{C}(x_{1}, \ldots, x_{d})\) represents the field of rational functions in \(\mathbb{C}^{d}\), which is the quotien field of \(\mathbb{C}[x_{1}^{\pm 1}, \ldots, x_{d}^{\pm 1}]\).</p>
  </li>
</ul>

<p>With this theorem, we can deduce the following lema,</p>

<p><strong>Lema.</strong> If a rational polyhedral cone \(\mathcal{K}\) is not strictly convex, then \(\rho(\mathcal{S_{\mathcal{K}}}) = 0\).</p>
<h4 id="4-connecting-all-together">4. Connecting all together</h4>
<p>We can now prove the Brion’s Formula for a simplex, and then extend it using irrational decomposition for the general case. A simplex is formed by the intersection of \(d+1\) halfspaces, with each facet corresponding to one of the halfspaces.</p>

<p><strong>Theorem 2.</strong> If \(P\) is a simplex, then
\begin{equation}
0 = \sum_{F} (-1)^{dim(F)} \mathcal{S_{\mathcal{K_{F}}}},
\end{equation}
the sum over all the faces of \(P\).</p>

<p>Now applying the valuation map \(\rho\) to the formula above, and we obtain the Brion’s Formula for a simplex.</p>

<p>Just as for rational cones, every polytope may be decomposed into simplices having pairwise disjoint interiors, using only the vertices of \(\mathcal{P}\). This way we can procceed to prove the Brion’s Formula for a general polytope.</p>
<h2 id="references">References</h2>

<p>This is a summary of the ideas developed in the famous work <a href="https://arxiv.org/pdf/math/0506466.pdf">Beck, M., Haase, C., &amp; Sottile, F. (2009). (Formulas of Brion, Lawrence, and Varchenko on rational generating functions for cones). The Mathematical Intelligencer, 31, 9-17.</a></p>

<p>[1] M. Brion, Points entiers dans les polyèdres convexes, Ann. Sci. École Norm. Sup. 21 (1988), no. 4, 653–663.</p>

<p>[2] A.N. Varchenko, Combinatorics and topology of the arrangement of affine hyperplanes in the real space,
Funktsional. Anal. i Prilozhen. 21 (1987), no. 1, 11–22.</p>

<p>[3] S. Verdoolaege, software package barvinok (2004), electronically available at http://freshmeat.net/projects/barvinok/.</p>]]></content><author><name></name></author><category term="sample-posts" /><category term="Combinatorics," /><category term="Discrete-geometry" /><summary type="html"><![CDATA[Summary and main concepts of the paper, Formulas of Brion, Lawrence, and Varchenko on rational generating functions for cones, by Beck, M., Haase, C., & Sottile, F. (2009).]]></summary></entry><entry><title type="html">Automatic Preconditioning by Limited Memory Quasi-Newton Updating</title><link href="http://localhost:4000/blog/2023/automatic_preconditioning_lm_q-Newton_Upd/" rel="alternate" type="text/html" title="Automatic Preconditioning by Limited Memory Quasi-Newton Updating" /><published>2023-01-27T00:00:00+01:00</published><updated>2023-01-27T00:00:00+01:00</updated><id>http://localhost:4000/blog/2023/automatic_preconditioning_lm_q-Newton_Upd</id><content type="html" xml:base="http://localhost:4000/blog/2023/automatic_preconditioning_lm_q-Newton_Upd/"><![CDATA[<h2 id="conjugate-gradient-cg">Conjugate Gradient (CG)</h2>

<p>Suppose we have some quadratic function,
\begin{equation}
f(x) = \frac{1}{2}x^{T}Ax-b^{T}x+c,
\end{equation}
for \(x\in \mathbb{R}^{n}\) with \(A\in\mathbb{R}^{n\times n}\) and \(b,c\in \mathbb{R}^{n}\).</p>

<ul>
  <li>
    <p>The gradient of \(f\) will be, \(\nabla f(x) = Ax - b\). Minimizing the previous convex problem is equivalent to solve \(Ax = b\).</p>
  </li>
  <li>
    <p>\(-\nabla f\) is the vector pointing the direction of steepest descent. We start with a initial guess \(x_0\) and we compute \(-\nabla f(x_{0})\), then we move in that direction by a step size \(\alpha\).</p>
  </li>
  <li>
    <p>We are interested in finding the best \(\alpha\). This is, we are want to find the \(\alpha\) such that it brings us to the minimum of \(f\) constrainted to the direction \(d_{0}=-\nabla f(x_{0})\).</p>
  </li>
  <li>
    <p>Computing \(\alpha\) is equivalent to minimizing the function \(g(\alpha) = f(x_{0}+ \alpha d_{0})\). The minimum of this function occurs when \(g'(\alpha)=0\). So we get that \(\alpha\) is,
\begin{equation}
\alpha = - \dfrac{d_{i}^{T}(Ax_{x_{i}} + b)}{d_{i}^{T}Ad_{i}}.
\end{equation}</p>
  </li>
  <li>
    <p>The second point in our iterative algorithm will be, \(x_{1}  = x_{0} - \alpha \nabla f(x_{0})\).</p>
  </li>
  <li>
    <p>We find a new direction \(d_1\) to move that is conjugate (w.r. \(A\)) to the previous one \(d_0\). So \(d_{1} = -\nabla f(x_1) + \beta_{0}d_{0}\). We can derive \(\beta_{0}\) from conjugacy, \(d_{1}^{T}Ad_{0}=0\).
\begin{equation}
\beta_{0}= \frac{\nabla f(x_{1})^{T}Ad_{0}}{d_{0}^{T}Ad_{0}}
\end{equation}
Iterating this process will keep giving us new conjugate directions.</p>
  </li>
</ul>

<h2 id="preconditioned-conjugate-gradient-method">Preconditioned conjugate gradient method</h2>

<p>The convergence analysis shows that convergence speed is fast when the condition number of \(A\) is close to \(1\). We can accelerate the convergence rate by replacing the system \(Ax=b\) by the preconditioned system,
\begin{equation}
M^{-1}Ax = M^{-1}b.
\end{equation}</p>

<p>The symmetric positive definite matrix \(M\) must be chosen s.t. the new system is solved with less computational work than the original one. That means that \(M^{-1}A\) has a more ‘favourable’ conditional number than \(A\).</p>

<h2 id="hessian-free-newton-method">Hessian-Free Newton Method</h2>

<p>We now have all the prerequisite background to understand the Hessian-free optimization method. The general idea behind the algorithm is as follows:</p>

<figure class="highlight"><pre><code class="language-c--" data-lang="c++"><table class="rouge-table"><tbody><tr><td class="gutter gl"><pre class="lineno">1
2
3
4
5
</pre></td><td class="code"><pre>    <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="n">be</span> <span class="n">our</span> <span class="n">initial</span> <span class="n">guess</span><span class="p">;</span>
    <span class="k">while</span><span class="p">(</span><span class="n">Stoping</span> <span class="n">test</span> <span class="n">not</span> <span class="n">satisfied</span><span class="p">)</span>
        <span class="n">At</span> <span class="n">x</span><span class="p">[</span><span class="n">n</span><span class="p">]</span> <span class="n">compute</span> <span class="err">∇</span><span class="n">f</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="n">n</span><span class="p">])</span> <span class="n">and</span> <span class="n">H</span><span class="p">(</span><span class="n">f</span><span class="p">)(</span><span class="n">x</span><span class="p">[</span><span class="n">n</span><span class="p">])</span> <span class="n">and</span> <span class="n">consider</span> <span class="n">taylor</span> <span class="n">expansion</span> <span class="n">of</span> <span class="n">f</span><span class="o">:</span>
            <span class="n">f</span><span class="p">(</span><span class="n">x</span><span class="o">+</span> <span class="err">Δ</span><span class="n">x</span><span class="p">)</span> <span class="err">≈</span> <span class="n">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="err">∇</span><span class="n">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span><span class="err">ᵀ</span> <span class="err">Δ</span><span class="n">x</span> <span class="o">+</span> <span class="err">Δ</span><span class="n">x</span><span class="err">ᵀ</span> <span class="n">H</span><span class="p">(</span><span class="n">f</span><span class="p">)</span> <span class="err">Δ</span><span class="n">x</span><span class="p">.</span>
        <span class="n">Compute</span> <span class="n">x</span><span class="p">[</span><span class="n">n</span><span class="o">+</span><span class="mi">1</span><span class="p">]</span> <span class="n">with</span> <span class="n">C</span><span class="p">.</span><span class="n">G</span><span class="p">.</span> <span class="k">for</span> <span class="n">quadratic</span> <span class="n">functions</span> <span class="n">on</span> <span class="n">the</span> <span class="n">Taylor</span> <span class="n">expansion</span><span class="p">.</span>
</pre></td></tr></tbody></table></code></pre></figure>

<h2 id="automatic-preconditioning-by-limited-memory-quasi-newton-updating">Automatic Preconditioning by limited Memory Quasi-Newton Updating</h2>

<p>We wish to accelerate the C.G. iteration used in Hessian-free Newton methods for nonlinear optimization.</p>

<h3 id="general-problem">General problem</h3>

<p>We want to solve the following problem,</p>

<p>\begin{equation}
A_{k}x = b_{k},\quad k=1,…,t,
\end{equation}</p>

<p>where \(A_{k}\) is the Hessian of the objective function at the current iterate. \(A_{k}\) vary slowly, and \(b_{k}\) are arbitrary.</p>

<p>We are also interested in solving finite element problems, so a sequence of linear systems,
\begin{equation}
Ax = b_{i}, \quad i=1, …, t.
\end{equation}
Both cases the coefficient matrices are symmetric and positive define.</p>

<h3 id="limited-memory-quasi-newton-algorithm">Limited memory quasi-Newton algorithm</h3>

<p>We deal with the large scale unconstrained optimization problem,
\begin{equation}
\min{f(x)},
\end{equation}
where \(f\) is a \(C^{2}\) of \(n\) variables.</p>

<p>Among the iterative methods for large scale unconstrained optimization,
when the Hessian matrix is possibly dense, limited memory quasi-Newton
methods are often the methods of choice.</p>

<p>They generate a sequence \({x_k}\), according to the following scheme:
\begin{equation}
x_{k+1} = x_{k} + \alpha p_{k}, \quad k = 0, …
\end{equation}
with \(p_{k} = -H_{k}\nabla f(x)\), where \(H_{k}\) is an approximation of the inverse of the Hessian matrix and \(\alpha_k\) is a steplength.</p>

<ul>
  <li>
    <p>Instead of computing \(H_{k}\) at each iteration \(k\), these methods update  \(H_{k}\) in a simple manner, in order to obtain the new approximation \(H_{k+1}\) to be used in the next iteration.</p>
  </li>
  <li>
    <p>Moreover, instead of storing full dense \(n \times n\) approximations, they only save a few vectors of length
\(n\), which allow to represent the approximations implicitly.</p>
  </li>
  <li>
    <p>L-BFGS method is usually considered very efficient. Well suited for large scale problems because
the amount of storage is limited and controlled by the user.</p>
  </li>
  <li>
    <p>This method is based on the construction of the approximation of the inverse of the Hessian matrix, by exploiting curvature information gained only from the most recent iterations.</p>
  </li>
</ul>

<p>So we have the following algorithm:</p>

<figure class="highlight"><pre><code class="language-c--" data-lang="c++"><table class="rouge-table"><tbody><tr><td class="gutter gl"><pre class="lineno">1
2
3
4
5
</pre></td><td class="code"><pre>    <span class="n">Solve</span> <span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="n">r</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span> <span class="o">&lt;-</span> <span class="n">A</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span><span class="n">x</span> <span class="o">=</span> <span class="n">b</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="n">with</span> <span class="n">unpreconditioned</span> <span class="n">CG</span><span class="o">-</span><span class="n">method</span><span class="p">;</span>
    <span class="k">for</span><span class="p">(</span><span class="n">i</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="p">...,</span> <span class="n">k</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">Compute</span><span class="o">/</span><span class="n">store</span><span class="o">:</span> <span class="n">s</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">=</span><span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">]</span><span class="o">-</span><span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="p">],</span>  <span class="n">y</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span><span class="n">r</span><span class="p">[</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">]</span><span class="o">-</span><span class="n">r</span><span class="p">[</span><span class="n">i</span><span class="p">],</span>  <span class="n">i</span> <span class="o">=</span> <span class="n">l</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="p">...,</span> <span class="n">l</span><span class="p">[</span><span class="n">m</span><span class="p">];</span>
        <span class="n">Define</span> <span class="n">BFGS</span> <span class="n">matrix</span> <span class="n">H</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="k">using</span> <span class="p">(</span><span class="n">s</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">y</span><span class="p">[</span><span class="n">i</span><span class="p">]);</span> <span class="p">(</span><span class="n">quasi</span><span class="o">-</span><span class="n">Newton</span> <span class="n">preconditioner</span><span class="p">)</span>
        <span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">r</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="o">&lt;-</span> <span class="n">Use</span> <span class="n">H</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="n">preconditioned</span> <span class="n">CG</span><span class="o">-</span><span class="n">method</span> <span class="n">to</span> <span class="n">solve</span> <span class="n">A</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="n">x</span> <span class="o">=</span> <span class="n">b</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
</pre></td></tr></tbody></table></code></pre></figure>

<ul>
  <li>The parameter \(m\) determines the amount of memory in the preconditioner.</li>
  <li>Two strategies to select  the \(m\) vectors: 1. Select the last \(m\) vector generated during C.G. iteration. 2. Take a uniform sample of them.</li>
  <li>We don’t need to compute the Hessian, we need to compute $H(m)v$, for any vector \(v\). Can be done by finite diferences (approximation).</li>
</ul>

<h2 id="results">Results</h2>
<p>We test the preconditioned-method in non linear optimization problems and in linear systems arising in finite element models.</p>
<ul>
  <li>In non linear optimization problems, there is a substantial reduction in the number of C.G. iterations, when \(m=8\). For beyond \(m=10\) most results are indentical to \(m=8\).</li>
  <li>For tight tolerance, the benefit can be modest. But for relaxed tolerance the saving number of C.G. iterations are important.</li>
  <li>With Finite element Matrices, there is also a reduction in the number of C.G. iterations, but there is no reduction of CPU time because matrices are very sparce.</li>
  <li>In general a uniform sampling strategy perform better than saving the last \(m\) pairs.</li>
</ul>

<h2 id="references">References</h2>

<p>This is a summary of the ideas developed in the famous work <a href="https://users.iems.northwestern.edu/~nocedal/PDFfiles/autoprecond.pdf">Updating, Q.N., 1999. Automatic Preconditioning by Limited Memory.</a></p>]]></content><author><name></name></author><category term="sample-posts" /><category term="optimization," /><category term="numerical-methods," /><category term="gradient-descent," /><category term="preconditioning" /><summary type="html"><![CDATA[Summary and main concepts of the paper Updating, Q.N., 1999. Automatic Preconditioning by Limited Memory.]]></summary></entry><entry><title type="html">Birth-Death process simulations and a Monte-Carlo dynamic algorithm (Gillespie)</title><link href="http://localhost:4000/blog/2022/Birth_Death_Process/" rel="alternate" type="text/html" title="Birth-Death process simulations and a Monte-Carlo dynamic algorithm (Gillespie)" /><published>2022-08-01T00:00:00+02:00</published><updated>2022-08-01T00:00:00+02:00</updated><id>http://localhost:4000/blog/2022/Birth_Death_Process</id><content type="html" xml:base="http://localhost:4000/blog/2022/Birth_Death_Process/"><![CDATA[<p>Repository <a href="https://github.com/josemanuel22/BirthDeathProcess.jl">BirthDeathProcess.jl</a>  process is a package in julia that implements a series of utilities to simulate the Birth-Death process, given by the differential equation \(n' = \beta - d\cdot n\).</p>

<p>Two algorithms are implemented for the resolution of this simple ODE. In the first place, a classical numerical integration algorithm of the <a href="http://users.uoa.gr/~tsitourasc/RK54_new_v2.pdf">Runge-Kutta</a> type and secondly, the well-known <a href="https://www.sciencedirect.com/science/article/pii/0021999176900413?via%3Dihub">Gillespie</a> Monte-Carlo dynamic algorithm is implemented.</p>

<h2 id="birth-death-process">Birth-Death Process</h2>

<ul>
  <li>
    <p>Simple model of population dynamics called a birth-death processes. Population fluctuations (birth and death rates, \(\beta_{birth}\) and \(\beta_{death}\)) depend only on the population size, \(n\).</p>
  </li>
  <li>
    <p>Let’s consider the following situation: protein synthesis. The rate at which the population increases depends only on the presence of the ingredients and is thus is fixed and a clearance rate (deaths) is proportional to the total population, \(n\).</p>
  </li>
  <li>
    <p>So we have two possible events: births (or synthesis) and deaths (or decays):</p>
  </li>
</ul>

<p>\begin{equation}
\beta_{birth} = (constant),\quad
\beta_{death}  = (k_0)*n.
\end{equation}</p>

<ul>
  <li>
    <p>\(\beta_{birth}\)  and \((k_0)\) are parameters of the model that would, in principle, be determined by experimental measurements. Both rates have units of [events/time]. From now on we will denote by \(d\) the parameter $k_0$.</p>
  </li>
  <li>Deterministically, the population size \(n\) can be described by the ordinary differential equation:
\begin{equation}
n’(t) = \beta - d \cdot n(t).
\end{equation}</li>
  <li>Solving this differential equation we get:
\begin{equation}
n(t) = c \cdot e^{-d\cdot t}+\beta/d,
\end{equation}
where \(c=n(0)-\beta/d\).</li>
</ul>

<h2 id="solution">Solution</h2>

<p>Simplest cases of pure birth process and pure death process.</p>

<ul>
  <li><em>Pure birth process</em>, the governing equation is, \(n(t)=b\cdot t + n(0)\). So any numerical solution that we obtain has to follow a linear pattern of \(\beta\) slope, for this case.</li>
  <li><em>Pure death process</em>, the  governing equation is, \(n(t)=e^{-d\cdot t}+n(0)-1\). In this case the decreasing exponential pattern has to be observed.</li>
</ul>

<p>General equation, we obtain the asymptotic trend. With previous solution for the general problem: \(n=\beta/d\). The concavity will depend on the sign of \(C\).</p>

<h2 id="stochastic-simulation-algorithm">Stochastic Simulation Algorithm</h2>

<ul>
  <li>
    <p>Two possible events might occur and these might happen at different rates: birth and death.
\begin{equation}
\frac{trial_{death}}{trial_{birth}+trial_{death}}= \frac{d\cdot N}{\beta + d\cdot N}, \qquad \frac{trial_{birth}}{trial_{death}+trial_{birth}}= \frac{\beta}{\beta + d\cdot N}
\end{equation}</p>
  </li>
  <li>
    <p><em>Bernoulli Trial</em>. A event can either be a birth or a death. So we need to determine which event have actually occured. For this porpuse we choose a random number between \((0,1)\) and see if it is less than \(\frac{d\cdot N}{\beta + d\cdot N}\) or bigger. In each case we add or subtract an individual to the population (Monte-Carlo simulation).</p>
  </li>
  <li>
    <p>For simplicity and to avoid divisions, we take a random number between \((0, d\cdot N+\beta)\), and we check that it is less than or greater than \(d\cdot N\).</p>
  </li>
  <li>
    <p>Events in birth-death processes do not occur at regular time intervals. We can only say that the odds of an event occurring within any small time interval \(\Delta t\) is equal to \(a_{0} \Delta t\), where \(a_0\) is the mean rate of event occurrences. A sequence of random wait times is an example of what is called a “Poisson Process.”</p>
  </li>
  <li>
    <p>Wait-times between sequential events follow an exponential distribution where the probability of the next event occurring after a duration \(\tau\) (give-or-take \(\Delta t\)) is given by:</p>
  </li>
</ul>

<p>\begin{equation}
P(\tau) = a_{0} \cdot e^{-a_0 \cdot \tau}
\end{equation}</p>

<ul>
  <li>By <em>superposition property</em> we get that \(\tau = - \frac{\log{r}}{\beta + d\cdot x}\). Where \(r\) is a random number between \((0,1)\).<br /><br /></li>
</ul>

<h2 id="results">Results</h2>

<ul>
  <li>We show different simulations (blue points) for different values of birth-rate and death-rate vs theoretical solution (red line). Blue line is the mean square approximation.</li>
</ul>

<p><img src="/assets/img/bithdeathproject/1_001_10.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_01_10.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_01_1_2.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_01_1_numerical.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_02_10.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_01_1.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_01_10_2.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_01_20.png" alt="hdsfjl" /></p>

<p><img src="/assets/img/bithdeathproject/1_05_1.png" alt="hdsfjl" /></p>

<p><br /></p>

<ul>
  <li>Multiple simulations with same parameters vs theoretical solution (red bold line).</li>
</ul>

<p><img src="/assets/img/bithdeathproject/1_01_1_multiple.png" alt="hdsfjl" /></p>

<h2 id="installation">Installation</h2>

<p>To install BirthDeathProcess, simply use Julia’s package manager. The module is not registered so you need to clone the repository and follow the following steps:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>julia&gt; push!(LOAD_PATH,pwd()) # You are in the BirthDeathProcess Repository
julia&gt; using BirthDeathProcess
</code></pre></div></div>

<p>To reproduce the enviroment for compiling the repository:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(@v1.6) pkg&gt;  activate pathToRepository/BirthDeathProcess
</code></pre></div></div>
<h2 id="presentation">Presentation</h2>

<p>In the presentation folder, there is a beamer with a short presentation on the birth-death process and the implemented code.</p>]]></content><author><name></name></author><category term="sample-posts" /><category term="Monte-Carlo," /><category term="numerical-methods," /><category term="simulation," /><category term="julia," /><category term="Gillespie" /><summary type="html"><![CDATA[Development of a dynamic montecarlo algorithm for the process of Birth-Death.]]></summary></entry></feed>